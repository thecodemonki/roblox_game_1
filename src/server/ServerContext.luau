-- Shared server registry: one mutable `Game` table passed to subsystem `mount(game)`.
-- Fields are filled by `init.server.luau` and each module (Chest, Ranked, Progression, Combat, Food).

export type Game = any

return {}
